<<<<<<< HEAD
object StringProcessor {
  def processStrings(strings: List[String]): List[String] = {
    // Было: var result = List[String]()  // изменяемая переменная
    // Нет явного объявления переменной, используется возвращаемое значение методов. Был цикл
    strings
      .filter(_.length > 3)
      .map(_.toUpperCase)
  }

  def main(args: Array[String]): Unit = {
    val strings = List("apple", "cat", "banana", "dog", "elephant")
    val processedStrings = processStrings(strings)
    println(s"Processed strings: $processedStrings")
  }
=======
object StringProcessor {
  def processStrings(strings: List[String]): List[String] = {
    // Было: var result = List[String]()  // изменяемая переменная
    // Нет явного объявления переменной, используется возвращаемое значение методов. Был цикл
    strings
      .filter(_.length > 3)
      .map(_.toUpperCase)
  }

  def main(args: Array[String]): Unit = {
    val strings = List("apple", "cat", "banana", "dog", "elephant")
    val processedStrings = processStrings(strings)
    println(s"Processed strings: $processedStrings")
  }
>>>>>>> aed20bf39d9bec1b42a00fdebe1947fb7ed3c7ee
}